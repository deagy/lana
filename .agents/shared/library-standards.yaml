go:
  preferred_stdlib:
    - net/http
    - encoding/json
    - fmt
    - log/slog
  preferred_third_party:
    - cobra (CLI framework)
    - zap (structured logging)
    - testcontainers-go (integration tests)
  linter: golangci-lint
  formatting: gofmt / goimports
